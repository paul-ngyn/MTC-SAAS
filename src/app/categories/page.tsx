// src/app/categories/page.tsx – All Products catalog (unfiltered)
import CatalogView from "@/components/CatalogView";

export const metadata = {
  title: "All Products — MTC Maple Trade Corporation Hub",
};

export default function CategoriesPage() {
  return <CatalogView />;
}
